# --- Secrets & local data (never commit) ---
*.env
.env
.env.*
config.toml
config.local.*
secrets.*
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
vault/
export-vault/
data/
certs/
*.pem
*.key
# Scraper login session jars (Playwright storageState cookie jars)
**/auth/*.json

# --- Go (server/, cli/, tooling/) ---
/server/bin/
/cli/bin/
/tooling/bin/
/server/sam
/cli/sam
*.test
*.out
coverage.*

# --- Node / Expo (app/, clipper/) ---
node_modules/
.expo/
.expo-shared/
dist/
web-build/
*.tsbuildinfo
.metro-health-check*
*.jks
*.p8
*.p12
*.mobileprovision
*.orig.*

# --- Clipper build output ---
/clipper/dist/
/clipper/*.zip

# --- App web build output ---
/app/dist/

# --- Claude Code internal state ---
.claude/

# --- Editor / OS ---
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# --- Temporary / debug (screenshots, browser sessions, scratch) ---
tmp/

# --- Logs ---
*.log
npm-debug.*
yarn-error.*
